name: test asset cost average

on:
  workflow_call:
  pull_request:
    branches:
      - main
    paths:
      - asset-cost-average/**

env:
  # github config
  PROJECT_DIR: asset-cost-average

jobs:
  test-project:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2 
        with:
          node-version: 16

      - name: Install dependencies
        run: |
          cd ./${{ env.PROJECT_DIR }}
          yarn install
    
      - name: Lint project
        run: |
          cd ./${{ env.PROJECT_DIR }}
          yarn lint

      - name: Run tests
        run: |
          cd ./${{ env.PROJECT_DIR }}
          yarn test